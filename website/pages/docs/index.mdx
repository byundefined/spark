# Get Started

Spark is a component toolkit to speed up Supabase development & integration into the start of any web-app

## Example

```jsx filename="app.jsx"
const App = () => {
  return <div>This is a demo</div>;
};
```

If you are looking into using it in a UI framework, check out the next section.

# Installation
